/api/v1/user/register:
  post:
    summary: 注册新用户
    tags:
      - User
    # ... 其他详细信息

/api/vi/user/register/captcha:
  get:
    summary: 获取注册验证码
    tags:
      - User
    parameters:
      - name: email
        in: query
        required: true
        schema:
          type: string
    responses:
      '200':
        description: 成功获取验证码
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  default: success
                code:
                  type: number
                  default: 200
                data:
                  type: boolean
                  default: true
      '400':
        description: Bad Request
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                  default: Email is required
                code:
                  type: number
                  default: 400
                data:
                  type: object
                  default: null

/api/v1/user/login:
  post:
    summary: 用户登录
    tags:
      - User
    # ... 其他详细信息
