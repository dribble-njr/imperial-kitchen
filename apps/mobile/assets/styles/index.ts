import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  hero: {
    marginBottom: 32
  },
  form: {
    gap: 16
  },
  screenContent: {
    flex: 1,
    padding: 24,
    gap: 16,
    overflow: 'hidden',
    borderRadius: 20
  }
});
