import { ThemedView } from '@/components/ThemedView';
import { Text } from 'react-native-paper';

export default function CreateKitchen() {
  return (
    <ThemedView className="flex-1 items-center justify-center">
      <Text>CreateKitchen</Text>
    </ThemedView>
  );
}
